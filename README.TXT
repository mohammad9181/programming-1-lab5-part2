Questions:

26: We check how many keys-paire there are. We can use the size() method.
28: We get an error message, becaue it doesn't know which to choose
(their the same).

29: the code works. because you can add 2 same values. as long as the
keys are different.

30: public boolean containsValue(Object value)

31: Nothing will happen. Unless there is a println.

32: We use keySet()

